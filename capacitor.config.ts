import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ifgoiano.minicurso.ionicangular',
  appName: 'TO-DO IF Goiano',
  webDir: 'www'
};

export default config;
